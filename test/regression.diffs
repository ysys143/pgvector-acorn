diff -U3 /workspace/test/expected/smoke.out /workspace/test/results/smoke.out
--- /workspace/test/expected/smoke.out	2026-06-07 08:16:02.582006671 +0000
+++ /workspace/test/results/smoke.out	2026-06-07 14:49:15.797975162 +0000
@@ -0,0 +1,34 @@
+-- smoke.sql: extension loads and basic objects are registered
+-- TDD: golden file starts empty; populate after implementation passes
+\set ON_ERROR_STOP on
+CREATE SCHEMA test_smoke;
+SET search_path = test_smoke;
+CREATE EXTENSION IF NOT EXISTS vector;
+CREATE EXTENSION IF NOT EXISTS pg_acorn;
+-- acorn_hnsw AM must be registered
+SELECT amname FROM pg_am WHERE amname = 'acorn_hnsw';
+   amname   
+------------
+ acorn_hnsw
+(1 row)
+
+-- GUCs must exist
+SHOW pg_acorn.enable_hook;
+ pg_acorn.enable_hook 
+----------------------
+ on
+(1 row)
+
+SHOW pg_acorn.default_gamma;
+ pg_acorn.default_gamma 
+------------------------
+ 1
+(1 row)
+
+-- basic table + index creation must not error
+CREATE TABLE items (id int, embedding vector(3));
+INSERT INTO items VALUES (1, '[1,0,0]'), (2, '[0,1,0]'), (3, '[0,0,1]');
+CREATE INDEX ON items USING acorn_hnsw (embedding vector_cosine_ops)
+  WITH (m = 4, ef_construction = 16, acorn_gamma = 1);
+ERROR:  acorn_hnsw index AM not yet implemented
+HINT:  Step 3 will implement the full acorn_hnsw AM.
diff -U3 /workspace/test/expected/tier1_hook.out /workspace/test/results/tier1_hook.out
--- /workspace/test/expected/tier1_hook.out	2026-06-07 08:16:02.582132338 +0000
+++ /workspace/test/results/tier1_hook.out	2026-06-07 14:49:15.963958112 +0000
@@ -0,0 +1,18 @@
+-- tier1_hook.sql: Tier 1 hook intercepts USING hnsw + WHERE and injects AcornScan
+-- TDD: golden file starts empty; populate after implementation passes
+\set ON_ERROR_STOP on
+CREATE SCHEMA test_tier1;
+SET search_path = test_tier1;
+CREATE EXTENSION IF NOT EXISTS vector;
+NOTICE:  extension "vector" already exists, skipping
+CREATE EXTENSION IF NOT EXISTS pg_acorn;
+NOTICE:  extension "pg_acorn" already exists, skipping
+SET pg_acorn.enable_hook = on;
+CREATE TABLE items (
+    id        serial PRIMARY KEY,
+    category  text,
+    embedding vector(4)
+);
+ERROR:  type "vector" does not exist
+LINE 4:     embedding vector(4)
+                      ^
diff -U3 /workspace/test/expected/tier2_am.out /workspace/test/results/tier2_am.out
--- /workspace/test/expected/tier2_am.out	2026-06-07 08:16:02.582207880 +0000
+++ /workspace/test/results/tier2_am.out	2026-06-07 14:49:15.994663743 +0000
@@ -0,0 +1,17 @@
+-- tier2_am.sql: acorn_hnsw index AM — creation, scan, planner selection
+-- TDD: golden file starts empty; populate after implementation passes
+\set ON_ERROR_STOP on
+CREATE SCHEMA test_tier2;
+SET search_path = test_tier2;
+CREATE EXTENSION IF NOT EXISTS vector;
+NOTICE:  extension "vector" already exists, skipping
+CREATE EXTENSION IF NOT EXISTS pg_acorn;
+NOTICE:  extension "pg_acorn" already exists, skipping
+CREATE TABLE items (
+    id        serial PRIMARY KEY,
+    category  text,
+    embedding vector(4)
+);
+ERROR:  type "vector" does not exist
+LINE 4:     embedding vector(4)
+                      ^
diff -U3 /workspace/test/expected/recall_filter.out /workspace/test/results/recall_filter.out
--- /workspace/test/expected/recall_filter.out	2026-06-07 08:16:02.582277839 +0000
+++ /workspace/test/results/recall_filter.out	2026-06-07 14:49:16.028306347 +0000
@@ -0,0 +1,19 @@
+-- recall_filter.sql: recall@10 vs brute force across filter selectivity levels
+-- ACORN must maintain recall >= 0.9 at all selectivity levels
+-- pgvector (seq scan) is measured as baseline for comparison
+-- TDD: golden file starts empty; populate after implementation passes
+\set ON_ERROR_STOP on
+CREATE SCHEMA test_recall_filter;
+SET search_path = test_recall_filter;
+CREATE EXTENSION IF NOT EXISTS vector;
+NOTICE:  extension "vector" already exists, skipping
+CREATE EXTENSION IF NOT EXISTS pg_acorn;
+NOTICE:  extension "pg_acorn" already exists, skipping
+CREATE TABLE items (
+    id        serial PRIMARY KEY,
+    bucket    int,        -- 0..99, filter = (bucket < N) gives N% selectivity
+    embedding vector(8)
+);
+ERROR:  type "vector" does not exist
+LINE 4:     embedding vector(8)
+                      ^
diff -U3 /workspace/test/expected/recall_gamma.out /workspace/test/results/recall_gamma.out
--- /workspace/test/expected/recall_gamma.out	2026-06-07 08:16:02.582346381 +0000
+++ /workspace/test/results/recall_gamma.out	2026-06-07 14:49:16.065121421 +0000
@@ -0,0 +1,17 @@
+-- recall_gamma.sql: gamma=2 must produce >= recall of gamma=1 under high selectivity
+-- TDD: golden file starts empty; populate after implementation passes
+\set ON_ERROR_STOP on
+CREATE SCHEMA test_recall_gamma;
+SET search_path = test_recall_gamma;
+CREATE EXTENSION IF NOT EXISTS vector;
+NOTICE:  extension "vector" already exists, skipping
+CREATE EXTENSION IF NOT EXISTS pg_acorn;
+NOTICE:  extension "pg_acorn" already exists, skipping
+CREATE TABLE items (
+    id        serial PRIMARY KEY,
+    bucket    int,
+    embedding vector(8)
+);
+ERROR:  type "vector" does not exist
+LINE 4:     embedding vector(8)
+                      ^
diff -U3 /workspace/test/expected/recall_insert.out /workspace/test/results/recall_insert.out
--- /workspace/test/expected/recall_insert.out	2026-06-07 08:16:02.582413381 +0000
+++ /workspace/test/results/recall_insert.out	2026-06-07 14:49:16.113509230 +0000
@@ -0,0 +1,18 @@
+-- recall_insert.sql: recall@10 must stay >= 0.85 after incremental inserts
+-- validates fixed-slot retry logic in acorn_build.c
+-- TDD: golden file starts empty; populate after implementation passes
+\set ON_ERROR_STOP on
+CREATE SCHEMA test_recall_insert;
+SET search_path = test_recall_insert;
+CREATE EXTENSION IF NOT EXISTS vector;
+NOTICE:  extension "vector" already exists, skipping
+CREATE EXTENSION IF NOT EXISTS pg_acorn;
+NOTICE:  extension "pg_acorn" already exists, skipping
+CREATE TABLE items (
+    id        serial PRIMARY KEY,
+    bucket    int,
+    embedding vector(8)
+);
+ERROR:  type "vector" does not exist
+LINE 4:     embedding vector(8)
+                      ^
diff -U3 /workspace/test/expected/no_regression.out /workspace/test/results/no_regression.out
--- /workspace/test/expected/no_regression.out	2026-06-07 08:16:02.582481881 +0000
+++ /workspace/test/results/no_regression.out	2026-06-07 14:49:16.151287324 +0000
@@ -0,0 +1,17 @@
+-- no_regression.sql: without a WHERE filter, acorn_hnsw must return the same
+-- top-k results as pgvector hnsw (unfiltered behavior must be identical)
+-- TDD: golden file starts empty; populate after implementation passes
+\set ON_ERROR_STOP on
+CREATE SCHEMA test_no_regression;
+SET search_path = test_no_regression;
+CREATE EXTENSION IF NOT EXISTS vector;
+NOTICE:  extension "vector" already exists, skipping
+CREATE EXTENSION IF NOT EXISTS pg_acorn;
+NOTICE:  extension "pg_acorn" already exists, skipping
+CREATE TABLE items (
+    id        serial PRIMARY KEY,
+    embedding vector(4)
+);
+ERROR:  type "vector" does not exist
+LINE 3:     embedding vector(4)
+                      ^
